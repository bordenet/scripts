# System files
.DS_Store
git_reset.log

# Packet capture artifacts
captures/*.pcap
captures/*.log
captures/*.pid

# AI assistant rules
.github/instructions/codacy.instructions.md
.cursor/rules/codacy.mdc

# Temporary/personal scripts
resume-at-0801.sh
delete-time-machine-snapshots.sh

# Malware sandbox - security sensitive files
analyze-malware-sandbox/id_rsa
analyze-malware-sandbox/id_rsa.pub
analyze-malware-sandbox/*.iso
analyze-malware-sandbox/shared/*
analyze-malware-sandbox/vm/*
analyze-malware-sandbox/.env

# Purge identity - runtime data
purge-identity/cache/*
purge-identity/logs/*
ADOPT-PROMPT.md
Agents.md.original
MODULAR-MIGRATION-PROMPT.md
*.backup

# Go build artifacts
# Compiled binary (go build ./cmd/gitsync/ drops this in the repo root)
/gitsync
/gitsync_new
# Test binaries produced by: go test -c ./...
*.test
# Coverage and profiling output: go test -coverprofile=coverage.out, -cpuprofile, etc.
*.out
# Vendor directory (go mod vendor)
/vendor/

# gitsync runtime files
.gitsync.hash
.gitsync.lock

# CI / review gate sentinels (local-only)
.code-review-cleared
.agent-gates.tmp
